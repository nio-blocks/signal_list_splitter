{
  "nio/SignalListSplitter": {
    "version": "0.1.0",
    "description": "Takes a list of signals, and notifies each signal in its own list with length 1.",
    "categories": [
      "Signal Flow",
      "Signal Inspection"
    ],
    "properties": {},
    "inputs": {
      "default": {
        "description": "Any list of signals."
      }
    },
    "outputs": {
      "default": {
        "description": "Same input signals but split each signal into its own list."
      }
    },
    "commands": {}
  }
}