{
  "nio/SignalListSplitter": {
    "version": "0.1.1",
    "description": "The SignalListSplitter is useful when the incoming signal is a list of signals. It will split the list into individual signals and emit each one as its own signal as a list of length 1.",
    "categories": [
      "Signal Flow",
      "Signal Inspection"
    ],
    "properties": {},
    "inputs": {
      "default": {
        "description": "Any list of signals."
      }
    },
    "outputs": {
      "default": {
        "description": "Same as the incoming signal list but split into individual signals."
      }
    },
    "commands": {}
  }
}
